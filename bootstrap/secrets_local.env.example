# Copy this file to secrets_local.env (same folder). That file is gitignored.
#
#   cp bootstrap/secrets_local.env.example bootstrap/secrets_local.env
#
# Then fill in real values on each machine (remote Jupyter, laptop, etc.).

HF_TOKEN=
DEEPSEEK_API_KEY=
